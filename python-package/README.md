# employee-events

`employee_events` is the reusable SQLite query package for the Employee Performance Dashboard. It exposes `Employee`, `Team`, and `QueryBase` models for retrieving names, performance-event totals, notes, and recruitment-model inputs.
